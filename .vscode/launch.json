{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(LLDB) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/a.out",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "g++ build active file"
        }
    ]
}
